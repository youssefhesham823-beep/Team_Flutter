import 'package:flutter/material.dart';

class CstmWidget extends StatelessWidget {
  const CstmWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
