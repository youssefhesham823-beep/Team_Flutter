import 'package:flutter/material.dart';

class CastmWidget extends StatelessWidget {
  const CastmWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
