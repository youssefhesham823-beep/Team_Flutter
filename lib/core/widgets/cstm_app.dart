import 'package:flutter/material.dart';

class CstmApp extends StatelessWidget {
  const CstmApp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
